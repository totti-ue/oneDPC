<div class="main">
  <div class="main-content second">
    <p class="sentence">ユーザー　一覧ページ ( nicknameをクリックすると、ユーザーページへ移動します )</p>
    <div class="main-content__box">


      <ul class="users-list">
        <% @users.each do |user| %>
          <li class="one-user">
            <a href="/users/<%= user.id %>", class="username"><%= user.nickname %></a>
          </li>
        <% end %>
      </ul>
      <%= link_to "ホームへ戻る", posts_path, class: "root-btn" %>
    </div>
  </div>
</div>