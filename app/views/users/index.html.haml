.main
  .main-content.second
    %p.sentence
    .main-content__box
      %ul.users-list
        - @users.each.with_index(1) do |user, index|
          %li.one-user
            = link_to "#{user.nickname}", "/users/#{user.id}"

    = link_to" 投稿一覧へ戻る", posts_path, class: ""
