.main
  .main-top
    .main-top__box
      %p.main-top__box--todays-theme
        %span{style: "font-weight:bold;"}
        本日のテーマ『
        = @today_theme.name
        』
      = render partial: "/shared/clock"
  %p#notice{style: "margin-top:120px; color:black;"}
    = notice

  .section-one
    





  -# .main-content
  -#   .main-content__box
  -#     %ul.box-list
  -#       - @posts.each do |post|
  -#         .additional
  -#           - if post.themes.first.id != @today_theme.id
  -#             %span.theme.wrong
  -#           - else
  -#             %span.theme.right
  -#               = @today_theme.name
  -#           = render partial: "post", locals: { post: post }
  -#       = paginate(@posts)

